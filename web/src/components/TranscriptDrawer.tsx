// RETIRED (topbar-collapse, 2026-08-16): the standalone transcript drawer
// was folded into the side drawer as its "transcript" (Log) tab — one
// drawer, one right edge, no exclusivity dance. Transcript.tsx (the
// content) lives on inside SideDrawer's tab body. Nothing imports this
// file; it exists only because the working tree couldn't unlink it at
// retirement time — safe to `git rm`.
export {};
