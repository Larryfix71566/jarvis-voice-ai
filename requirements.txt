pipecat-ai[deepgram,elevenlabs,openai,google,silero,mcp,runner,webrtc]
mcp[cli]
fastmcp>=3.4.5
openai
pydantic-settings
pyyaml
psutil
tavily-python
httpx
python-dateutil
openwakeword
websockets
# Credential vault (MORTIMER_CREDENTIAL_VAULT_PLAN.md S9). Both were
# already transitive deps (pinned in requirements-lock.txt); listed
# directly so a future lock regeneration can never drop them.
cryptography
keyring
pytest
pytest-asyncio
pytest-cov
ruff

# --- Optional: server-side noise suppression (voice isolation plan, Workstream A) ---
# Uncomment ONE engine, then set JARVIS_NS_ENABLED=true in .env:
# deepfilternet      # DeepFilterNet 3 (best quality; pulls torch, ~2 GB)
# pyrnnoise          # RNNoise (lightweight fallback)
# pystoi             # eval harness metric (scripts/eval_ns.py)
