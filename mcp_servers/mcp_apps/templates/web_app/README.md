# __APP_NAME__

__DESCRIPTION__

Built by Mortimer on __CREATED_AT__.

## Run

Open `src/index.html` in a browser, or serve the folder:

```bash
python3 -m http.server 8080 --directory src
```

Then visit http://localhost:8080.
