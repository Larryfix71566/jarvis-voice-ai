# Skill manifest (upgrade plan §3.1)
name: mcp-web
version: 1.0.0
class: standard
tools: [web_search, get_weather, get_weather_radar, research_compare_start, research_status, research_save]
requires_env:
  - TAVILY_API_KEY
# get_weather defaults to imperial; explicit settings are still forwarded.
optional_env: [JARVIS_UNITS]
requires_keychain: []
scopes: []
test: "python3 -m pytest tests/unit/test_mcp_web_logic.py -q"
