# Extracted from memory fact 'user.preference.api_research' on conversion (K6.2).
# REVIEW ME: `when` decides what this matches and `done_when` is
# what makes it more than a note. Both are drafts.
# draft: true — listed in the workflow viewer, never matched
# (MORTIMER_WORKFLOW_VIEWER_PLAN.md D-V1, Larry 2026-09-25).
draft: true
name: research-before-modifying
when: "Research alternative APIs first before modifying code"
steps:
  - Research alternative APIs first before modifying code
done_when: []
