# Consolidated from 1 memory fact(s) on conversion (K6.2).
name: give-exact-commands
when: "fix run command how do I terminal shell instructions"
steps:
  - Give the exact command to run rather than an explanation of what to do.
  - Larry executes directly; prose costs him a translation step.
done_when:
  - A copy-pasteable command was provided
# source facts:
#   user.style.direct_action
