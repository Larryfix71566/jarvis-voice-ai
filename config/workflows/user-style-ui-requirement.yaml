# Extracted from memory fact 'user.style.ui_requirement' on conversion (K6.2).
# REVIEW ME: `when` decides what this matches and `done_when` is
# what makes it more than a note. Both are drafts.
name: user-style-ui-requirement
when: "Analyst agent must appear in agents window in sidecar panel."
steps:
  - Analyst agent must appear in agents window in sidecar panel.
done_when: []
