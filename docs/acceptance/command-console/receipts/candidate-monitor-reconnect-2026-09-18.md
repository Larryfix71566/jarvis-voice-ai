# Candidate monitor reconnect acceptance — 2026-09-18

After the physical unplug receipt, the external `C34H89x` was reconnected.
The topology probe again reported two screens: built-in Retina plus the
external display at `3440x1440`. The main console retained the graph and
response while the display was absent. Opening the display after reconnect
restored one supporting-stage window with the same two bounded results:
`Memory graph` and `Mortimer response`.

No nested information window or duplicate stage was created. The candidate
was still in `ERROR VOICE` (`Socket is not connected`), so this receipt closes
display topology and bounded-stage restoration only; live voice/reconnect
acceptance remains open.
