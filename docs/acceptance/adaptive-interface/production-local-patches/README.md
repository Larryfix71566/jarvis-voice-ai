# Production local-patch inventory (closure plan C0.2, gap G29)

Captured 2026-09-11 from the installed checkout `~/jarvis-voice-ai-clean` at HEAD `2ccf66c` with `git diff -- <file>`. The five diffs beside this file are verbatim. A scan for credential-shaped strings (`api_key`, `token`, `password`, `secret`, `sk-…`) across all five found nothing.

**Larry decides each row before C10 step 2.** The implementer has recorded what each patch does and a recommendation; the decision column is blank until Larry fills it.

| File | Diff | What the patch does | Covered by candidate? | Recommendation | Larry's decision |
|---|---|---|---|---|---|
| `macos/MortimerHost/scripts/bundle.sh` | +14 −3 | Copies every SPM-built `*.framework` beside the binary; exits 1 if `WebRTC.framework/WebRTC` is missing; explains the `@loader_path`-only rpath (checked 2026-09-08) | **Yes, fully.** `bundle.sh.production-vs-candidate.diff` shows the candidate has both behaviors (candidate `:33-39`) and adds nested signing + `codesign --verify --deep --strict` (`:65-72`). Only comment wording differs. | Discard the local patch at C10.2 (`git checkout -- macos/MortimerHost/scripts/bundle.sh`) after `main` carries the candidate version. This is locked decision L2. | |
| `jarvis/bot/progress_watcher.py` | +22 −11 | Replaces `_last_tool_for_run` with `_live_progress_for_run` — counts `tool_result` events live instead of reading `agent_runs.tool_count` (written only at run end), so the 30 s narration no longer says "0 tool calls in, last was X". Motivated by Larry disconnecting mid-self-edit on 2026-09-08. | No — `jarvis/` is untouched by the candidate. | Commit to `main` in its own small PR ("progress watcher: live tool count"). No test references either function name (grep of `tests/`), so add one unit test for `_live_progress_for_run` with a temp DB in that PR. Not part of this plan's file boundary — a separate backend PR. | |
| `jarvis/admin/server.py` | +6 −2 | `_run_finish` log line now includes `notice=%r` (the git/GitHub error text) so a failed submit leaves a record of why. | No. | Commit to `main` in the same small backend PR as above (both are 2026-09-08 observability fixes). Note `jarvis/admin/**` is on the self-edit deny list — this must be a human commit, which it will be. | |
| `jarvis/prompts.py` | +2 −2 | Supervisor rule 9 and the developer `self_development` section: a self-edit no longer waits for a spoken "yes" after the preview — the Supervisor speaks the preview and confirms in the same turn; the PR is where approval happens. | No. | **Larry's call on behavior, not just housekeeping.** This changes the voice confirmation policy. `tests/unit/test_prompts.py` asserts on rule 9's wording (around line 666) — run that file before committing. Either commit as its own PR ("prompts: self-edit preview-then-confirm") or discard if the earlier explicit-yes policy is preferred. | |
| `scripts/launchd/com.mortimer.template.plist` | +1 −1 | Adds `/usr/sbin:/sbin` to the launchd `PATH`. | No. | Commit to `main` (trivial, and `tests/unit/test_launchd_gen.py:131` only asserts the log path, unaffected). Could ride the backend PR above. | |

Working-tree rule for C10: the installed checkout must be clean (`git status --porcelain` empty) before `git pull`. Each row above ends either in a merged commit on `main` or a `git checkout --` of the file. Nothing is stashed.
