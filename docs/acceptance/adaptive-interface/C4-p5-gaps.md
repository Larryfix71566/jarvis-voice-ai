# C4 — P5 gap closure record

Date: 2026-09-12. Candidate base `6bf0270` + uncommitted C1–C4 edits (40 dirty paths). Verified on the MacBook Air (Mac17,4, macOS 26.6.2, Swift 6.3.3) via `closure-checks/run-host-c4b.command` → `closure-checks/logs/host-c4b.log`, SHA-256 `a56f4c33795f521d8f7b54b8246ea8f855eed0e38921e9ca16a91afdea98a515`.

Result: **MortimerHost 148 tests, 0 failures, exit 0** (143 after C3 + 5 new in `ScreenPlacementTests`). Warnings: only the four pre-existing `accessibilitySetValue` deprecations. The first attempt, `host-c4.log` (also 148/0), added two isolation warnings from the new test's helper class; the helper and the two injected closure types were made `@MainActor` and the suite re-run — no assertion changed. The P4 frame-time gate re-ran in both runs (p95 9.2 ms and 11.7 ms; JSON refreshed).

| Gap | Change | Evidence |
|---|---|---|
| G16 unlock | `Placement/ScreenPlacement.swift`: `NSWorkspace.screensDidWakeNotification` and the distributed `com.apple.screenIsUnlocked` (loginwindow's unlock signal; there is no `NSWorkspace` equivalent) join `didWake` and `sessionDidBecomeActive`. All four, and `didChangeScreenParameters`, funnel through `topologyChanged()` → the single debounced work item with `topologyChanged: true`. | `testWakeSessionScreensAndUnlockSignalsAllScheduleATopologyReposition`: each of the three workspace notifications and the distributed unlock notification schedules exactly one reposition; the pending item carries the topology flag (recovery time is recorded when it runs). The test posts the unlock name on the distributed centre once — other processes listening for it will see one synthetic unlock. Hardware lock/unlock remains the §9.4 row for C8. |
| G17 scale | `PlacementScreen.scale` **removed** (not used): AppKit window frames and `visibleFrame` are in points on every display, so a mixed-scale setup needs no point/pixel clamp; the 300×200 minimum in `DisplayPlacementPolicy.clamp` is already in points. `liveScreens()` no longer reads `backingScaleFactor`; the three test fixtures dropped the argument. | Compiles with no remaining reference (`grep scale Placement/` → only the explanatory comment); `DisplayPlacementPolicyTests` unchanged in substance and green. |
| G18 adapter seams and tests | `ScreenPlacement.init(defaults:screens:windows:schedule:now:)` injects the defaults store, screen list, window lookup, debounce scheduler and clock; `shared` wires the live ones (`liveScreens()`, `findHostWindow`, `DispatchQueue.main.asyncAfter`, `systemUptime`). `decodeRecords` is a static, testable step; `records`/`placed`/`isApplying`/counters exposed read-only. Behaviour otherwise unchanged. | `testBurstOfSignalsCollapsesIntoOneRepositionThatRemembersTopology` (three schedules → two cancelled items, one reposition; a topology flag inside a burst survives); `testAppliedMovesAreNotRecordedAsManualButLiveResizeIs` (an off-screen display window is recovered by `setFrame` with `manual == false`; a programmatic move without pointer/live-resize evidence stays automatic; `didEndLiveResize` makes it manual, revision 1, persisted; a console live-resize schedules a reposition). Note: the `applying` guard and the pointer/live-resize evidence rule are both on that path, so the test exercises them jointly — a synthetic notification with fake pointer evidence is not producible without an `NSEvent` seam, which this closure did not add. `testPersistedRecordsDecodeOrAreRejectedFieldByField` (bad frame, negative revision, 256-char screen id, invalid recovery, unknown kind, foreign version, non-JSON, nil, 50 000-byte blob; a manual record restores a reopened window to its saved frame). `testResetLayoutClearsOnlyPlacementState` (records cleared to automatic placement, placed frames rebuilt, persisted; sidecar text size, layout version and graph-view keys in the same defaults untouched — the drawer width reset is the menu action's own line in `MortimerHostApp.swift:131`). |
| G19 recovery timing | Clock seam: `topologyObservedAt` is set on the first topology-class signal of a burst; when the debounced reconciliation runs, `lastTopologyRecoverySeconds` and `lastTopologyRecoveryMoves` are recorded and one line is logged: subsystem `com.mortimer.host`, category `placement`, "topology recovery: N window(s) moved, M ms after the first signal". | Fake-clock assertion in the debounce test (0.3 s from first signal to reconciliation, 0 moves). **Hardware measurement is a C8 row**: unplug/replug, lock/unlock and sleep/wake on the deployment Mac, read with `log show --last 10m --predicate 'subsystem == "com.mortimer.host" AND category == "placement"'`; gate ≤ 1 s. The 250 ms debounce is a floor on every reading. |

Contracts: UI-5 — placement state still has one owner (`ScreenPlacement`), the seams are constructor inputs, not new state; §5 Reset Layout semantics preserved; UI-7 — no skip/xfail.

Open for C8: §9.4 lock/unlock, sleep/wake and hot-plug rows with the log line above; visual check that the unlock signal does not move a window the user placed.

PR: "P5b — placement adapter tests and unlock reconciliation" (C9 step 3).
