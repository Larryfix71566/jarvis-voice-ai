# C2 — P3 gap closure record

Date: 2026-09-12. Candidate base `6bf0270` + uncommitted C1/C2 edits. Verified on the MacBook Air host toolchain via `closure-checks/run-host-c2c.command` → `closure-checks/logs/host-c2c.log`, SHA-256 `6648819860c0418b8efcbf3606fcaa24ec1d17b1213d4980026cab2d639712d4`.

Result: **138 tests, 0 failures, exit 0** (130 after C1 + 8 new in `AdaptiveInterfaceClosureC2Tests`). Two earlier attempts (`host-c2.log`, `host-c2b.log`) failed one assertion — the overlap-label rendering test looked for an accessibility node that SwiftUI does not create for a lone `Text`; the assertion was replaced by a rendered-pixel check (violet pixels present during overlap, absent otherwise). No production behavior changed between attempts other than the label's explicit accessibility element; no assertion was weakened.

| Gap | Change | Evidence |
|---|---|---|
| G05 rail parity (wake ripple, beams) | `Console/OrbFieldView.swift` `compactReadout`: `WakeRipple(diameter: 72)` replays over the readout on every wake pulse (labelled "Wake word detected"); each satellite row gets a `compactBeam` — an accent bar lit while the agent works and fading over 2.4 s after completion, from the same `isWorking`/`lastCompleted` truth as the full-stage beams, accessibility label "<Agent> working" / "<Agent> finished". | `testCompactRailRendersAgentActivityAffordance` hosts `OrbFieldView(compactPresentation: true)` with a synthetic working run decoded through `AppMessage` and finds "Developer working" plus the satellite in the AX tree. |
| G06 barge-in label | `App/VoicePresentationState.overlapLabel` = "Mortimer still speaking" iff `activity == .user && assistantSpeaking`; new `VoiceOverlapLabel` view rendered in violet inside `centerReadout` (shared by full and compact presentations). | `testOverlapLabelIsDerivedAndRendered`: property for overlap / non-overlap / plain assistant speech, and rendered violet pixels > 20 during overlap, 0 without. |
| G07 200 ms transition | `AppTuning.layoutTransitionSeconds = 0.2`; `AdaptiveTransition.animation(reduceMotion:)` returns nil under Reduce Motion, while a pointer button is held, or while an `NSTextView` is first responder; `AdaptiveStageView` now computes an explicit `AdaptiveLayoutMode` (`conversation`/`rail`/`bottom`) and animates on it; `WorkspaceView` animates comparison and graph entry/exit. | `testTransitionIsSuppressedUnderReduceMotion`; mode function covered by the G09 test. |
| G08 tuning in one place | `AppTuning`: `wideLayoutMinWidth 1180`, `inspectorSubpaneMinWidth 820`, `inspectorWidth 300`; new `AudioPresentationTuning`: user/assistant/neutral RGB (#2DD4BF / #A78BFA), attack 0.040 s, release 0.180 s. `ConsoleView`, `WorkspaceSourcesView`, `VoiceWaveView`, `VoicePresentationState.VoiceEnvelope` reference them; no literals remain. | `testTuningConstantsMatchInterfacePlanSection7`, `testEnvelopeUsesTheTunedAttackAndRelease` (1−1/e after one attack constant; e⁻¹ decay after one release constant). |
| G09 large-wave rule | Interface plan §4.1 amended (the "amend" branch): the large wave means no result is *presented*; returning to conversation keeps the active result selected so "Return to workspace" resumes. `AdaptiveStageView.mode(...)` is the single rule. | `testLargeWaveOnlyWhenReturnedToConversationAndReturnKeepsTheActiveResult`: first result enters workspace; rail vs bottom by width and `minimumRailStageWidth`; return keeps `activeID`; compact preference keeps the rail; an arrival during conversation is unread history, never a mode change. |
| G10 production limits | No source change. | `testProductionPinLimitOfTwentyRefusesTheTwentyFirstWithoutEviction` (default store: 20 pins accepted, 21st refused, pins survive 30 unpinned arrivals); `testWindowSurfaceResultsShareTheWorkspaceIdentity` (`DisplayWindowPanel.workspaceID` equals the workspace id and survives closing the central tab). |

Contracts: UI-2 untouched (no audio path change); UI-3 — routing and stores unchanged, only presentation; UI-5 — mode is derived, not stored; UI-7 — no skip/xfail; all 32 suites green.

Open for C8: contrast verification of teal/violet on the actual theme; visual confirmation of the compact ripple/beam on hardware; §9.2 rows.

PR: "P3b — workspace gap closure" (C9 step 3).
