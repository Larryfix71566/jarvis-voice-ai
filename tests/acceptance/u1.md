# U1 Execution Card — Framework formalization

Plan refs: upgrade plan §3, §10 U1. Invariants touched: 9 (logic/server split — preserved, no changes to logic.py files).

## File scope (only these may be touched)

- `mcp_servers/*/skill.yaml` (new)
- `scripts/check_skills.py` (new)
- `tests/unit/test_check_skills.py` (new)
- `tests/acceptance/u1.md` (this file)

## Gates

### G1 — manifests + validator pass

```
python3 scripts/check_skills.py --run-tests
```
Expected: `SKILLS OK` summary line listing 5 skills; exit code 0.

```
python3 -m pytest tests/unit/test_check_skills.py -q
```
Expected: all pass.

### G2 — roster-only agent addition validates

Covered by unit test `test_extra_agent_roster_only_validates` (adds a fifth agent
referencing existing skills against a temp config → validator passes;
referencing a missing skill → validator fails). No code changes made.

### G3 — hot reload (reconnect picks up new skill, no bot restart)

Deferred to live verification on the Mac (requires running bot + WebRTC
client): add a scratch standard skill, reconnect console, confirm the tool
is callable without restarting the bot process. Evidence to be pasted below.

- [ ] G3 live evidence (Mac): ___

## Evidence

### G1 (sandbox, 2026-08-07)

```
$ python3 scripts/check_skills.py --run-tests
SKILLS OK — 5 skills validated (tests run)   (exit 0)

$ python3 -m pytest tests/unit/test_check_skills.py -q
8 passed in 0.19s

$ python3 -m pytest -q
194 passed, 3 skipped in 56.83s   (gate 186 + 8 new; monotonicity holds)
```

Note: gate run surfaced a wiped-sandbox issue (NLTK punkt_tab data missing →
pipecat import blocked on network). Re-seeded from mirror; unrelated to U1 code.

### G2

`test_extra_agent_roster_only_validates` passes — roster-only fifth agent
validates without code changes; bad skill reference is rejected.
