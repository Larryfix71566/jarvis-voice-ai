# U1.5 Execution Card — Git skill + console Git panel (privileged pilot)

Plan refs: upgrade plan §10 U1.5, §5.2 (draft/commit), §9.3 (allowlist).
Invariants: 6 (allowlist — no force-push/deletion/direct-main tools exist),
9 (logic.py transport-free), 2 (gate monotonic: 194 → 208).

## File scope

- `jarvis/db.py` (migration 0002_actions appended — additive only)
- `mcp_servers/mcp_git/{__init__.py, logic.py, server.py, skill.yaml}` (new)
- `jarvis/admin/{__init__.py, server.py}` (new)
- `scripts/run_admin.sh` (new)
- `config/agents.yaml` (developer agent appended — roster change only)
- `jarvis/prompts.py` (developer specialist prompt appended)
- `web/src/components/GitPanel.tsx` (new), `web/src/App.tsx` (panel wiring),
  `web/src/App.css` (panel styles appended)
- `tests/unit/test_mcp_git_logic.py`, `tests/unit/test_admin_api.py` (new)
- `tests/unit/test_db.py`, `test_delegate.py`, `test_subagent.py`,
  `test_prompts.py` (locked expectations updated for new roster/migration)
- `tests/acceptance/u15.md` (this file)

## Gates

### G1 — read tools answer by voice
Covered structurally by unit tests here; live voice check deferred to Mac
(same pattern as U1-G3): "Jarvis, what's the repo status?" → Developer agent.

### G2 — draft → confirm → commit/push with audit rows

```
python3 -m pytest tests/unit/test_mcp_git_logic.py -q
```
Expected: all pass — incl. single-use drafts, expiry, audit rows.

### G3 — sidecar admin API + console panel

```
python3 -m pytest tests/unit/test_admin_api.py -q
```
Expected: all pass — endpoints are thin pass-throughs over mcp_git.logic.

```
cd web && npm run build
```
Expected: build succeeds.

Live (Mac): `bash scripts/run_admin.sh` in a second terminal; console shows
Repository panel with branch/status; Draft → Confirm commit works from the panel.

- [ ] G3 live evidence (Mac): ___

## Notes / wiring decisions

- Draft TTL 15 min; drafts single-use; expiry lazily marked on read.
- Panel polls `/api/git/status` every 15 s; shows "admin sidecar offline"
  when 7861 is down (never blocks the console).
- Sidecar binds 127.0.0.1 only; docs/openapi endpoints disabled.
- §5.4's structural new-turn precondition is U3 scope; this pilot enforces
  draft/commit at the action layer (id + single-use + expiry + audit).

## Evidence

### G2/G3 + full gate (sandbox, 2026-08-07)

```
$ python3 -m pytest tests/unit/test_mcp_git_logic.py tests/unit/test_admin_api.py -q
14 passed in 1.35s

$ python3 scripts/check_skills.py --run-tests
SKILLS OK — 6 skills validated (tests run)   (mcp-git now registered)

$ cd web && npm run build
✓ built

$ python3 -m pytest -q
208 passed, 3 skipped in 56.81s   (194 + 14 new; monotonic)
```

Locked-expectation updates (plan-sanctioned by U1.5 roster + migration additions):
test_db.py expected tables/migrations now include actions + 0002_actions;
test_delegate.py / test_subagent.py / test_prompts.py rosters now include the
developer agent. No tests removed.
